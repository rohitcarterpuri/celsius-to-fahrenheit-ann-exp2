# Celsius to Fahrenheit ANN Converter

A simple neural network that learns to convert Celsius to Fahrenheit.

## Usage

```bash
# Install requirements
pip install -r requirements.txt

# Train the model
python train.py

# Use the model in your code
from model import predict
print(predict(25))  # Output: 77.00
